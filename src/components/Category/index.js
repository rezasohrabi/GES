import React from 'react';
import { Grid } from '@material-ui/core';

const Category = props => {
    return (
        <Grid>
            hello category
        </Grid>
    )
}

export default Category;
